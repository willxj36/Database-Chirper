import { Query } from './index';

const post = async (userid: string, chirpid: string) => {
    try {

    } catch(e) {
        console.log(e);
    }
}

const deleter = async () => {
    try {

    } catch(e) {
        console.log(e);
    }
}

export default {
    post,
    deleter
}